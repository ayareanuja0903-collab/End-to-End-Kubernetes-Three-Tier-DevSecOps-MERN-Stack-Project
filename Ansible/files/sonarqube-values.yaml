community:
  enabled: true

monitoringPasscode: "sonar123"

service:
  type: LoadBalancer

persistence:
  enabled: true
  storageClass: gp2
  accessMode: ReadWriteOnce
  size: 10Gi

postgresql:
  enabled: true
  auth:
    username: sonar
    password: sonar
    database: sonarqube

resources:
  requests:
    cpu: 500m
    memory: 2Gi
  limits:
    cpu: 1000m
    memory: 4Gi
